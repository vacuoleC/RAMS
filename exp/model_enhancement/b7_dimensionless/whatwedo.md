# B7 去量纲化目标对比 · 探索标记（whatwedo.md）

- **标记时间**：2026-08-10 11:36（东八区）
- **任务**：B7 去量纲化目标对比（exp/model_enhancement/b7_dimensionless/）
- **背景**：B2 增量分位数区间校准好（覆盖 0.824）但有 2 个弱点——窗口间方差大（高波动窗口欠覆盖）、p50 方向判别弱（0.540≈随机）。假设藻类浓度是乘性过程，绝对 Δ 尺度随季节水平走；换成无量纲相对/对数目标可能修复
- **探索依据**：exp/EXPLORATION_POLICY.md（文件级探索标记）；同一滚动窗口协议（训练 730d/测试 90d/步长 45d）
- **方法**：H100（sensecore）同一协议下对比 4 变体（全部分位数头 p10/p50/p90 + GRU 骨干 + 输入含 conc_t）：abs_delta（B1/B2 基线）/ rel_delta / log_ratio（log1p 差）/ abs_log（对照）；全部还原 conc 单位评估，各自口径持久化对照；覆盖率（重点看窗口间方差）+ 每视界 CRPS + p50 RMSE + 方向命中率
- **产物**：
  - `run_b7.py`（实验脚本，冒烟通过）
  - `trydoing.jsonl`（执行日志）
  - `rethinking.md`（思考记录）
  - `results.md`（结论）
  - `results.json`（统计量，无原始数据）
- **合规**：只输出统计量/覆盖率/CRPS/RMSE，不打印原始数据行；不触碰 rams/ 冻结代码（仅 import 复用）；数据零值用 log1p 处理
- **状态**：✅ 探索完成（2026-08-10 11:33，H100 全量 17 窗口 × 4 变体 × 30 epoch，7.4 分钟）
- **结论摘要**：去量纲化**部分修复** B2 弱点——覆盖 rel 0.815 / log 0.813 vs abs 0.794（更接近 80%），覆盖窗口 std abs 0.101 → rel 0.091 / log 0.085（-16%）；CRPS 三者打平（相对各自持久化 abs +20.6% / log +19.0% / rel +18.5%）；但 **方向判别未修复**（全部 0.53-0.54 ≈ B2 的 0.540 ≈ 基线），且去量纲化在低浓度段覆盖更差（abs 0.602 → rel 0.557 / log 0.534）；abs_log 彻底失败（-44.2% 负技能，确认"增量项"是 B2 优势来源）。详见 results.md。
- **边界决策**：正式模型建议保留 abs_delta（CRPS/RMSE 最优、低浓度校准最好、实现最简）；若预警偏重相对涨落百分比可换 log_ratio（校准窗口方差最小，代价是低浓度段不确定放大）；方向预警需另配 P(Δ>0) 概率输出 + 阈值校准（与目标口径无关）——待主会话审核
