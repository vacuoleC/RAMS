# J 中长程慢变量特征 · 探索标记（whatwedo.md）

- **标记时间**：2026-08-10 14:30（东八区）
- **任务**：J 中长程慢变量特征（`exp/model_enhancement/j_slow_vars/`）
- **背景**：当前输入是 T=24（3 天）窗口 + conc_t，只有短期记忆；M5 证实藻类 ACF 衰减需
  75-90 天（强季节性/长记忆）。短板识别：特征缺"中长程季节状态"——可能同时是方向判别弱
  （短板2，B_direction AUC≈0.59）与区间校准漂移（短板1，H 窗口 std 0.09）的根因
- **探索依据**：exp/EXPLORATION_POLICY.md（文件级探索标记）；同一滚动窗口协议
  （训练 730d/测试 90d/步长 45d，17 窗口）
- **方法**：H100（sensecore）同一协议下对比 2 变体 × 3 seed：base（现输入，与 A/B7 同口径）
  vs base+慢变量（conc_0.5 + air_temp 过去 30/60/90 天滚动均值/斜率/方差，共 15 通道）；
  全部还原 conc 单位评估；CRPS/RMSE + 覆盖（看窗口 std）+ 方向 AUC（Φ(q50/σ) 反推，B_direction 口径）
- **防泄漏**：慢变量只用过去数据（shift(1)+rolling 因果）；窗口训练段标准化；头部预热 NaN
  用 ffill+常数扩展（只落第 1 窗口训练段）
- **产物**：`run_j.py`（实验脚本，冒烟通过）／`trydoing.jsonl`／`rethinking.md`／
  `results.md`（待全量后写）／`results.json`（统计量，无原始数据）
- **合规**：只输出统计量/覆盖率/CRPS/RMSE/AUC，不打印原始数据行；不触碰 rams/ 冻结代码
  （仅 import 复用）
- **状态**：✅ 探索完成（2026-08-10 15:16，H100 全量 17 窗口 × 2 变体 × 3 seed × 30 epoch，25.7 分钟）
- **结论摘要**：慢变量**只帮短板1的窗口 std（-6.5%），伤短板2 + 点精度**——CRPS 0.891→0.906
  （+1.71%，不显著）、p50RMSE 1.774→1.815（+2.25%，Wilcoxon p=0.023 显著变差）、方向 AUC
  0.581→0.564（-0.016，p=0.006 显著变差，17/14 窗口变差）、覆盖 0.806→0.815（p=0.43 不显著，
  窗口 std 0.0875→0.0818 即 -6.5%）。方向判别弱**不是缺季节状态**造成的（慢变量与 24h Δ 相关
  全部 ≤0.037）；校准稳定性线索保留（把年际状态喂给不确定性估计作独立校准层，如 H seas_reg
  用慢变量替代月因子）。base 逐项复现 A 多任务（CRPS 0.8912/RMSE 1.7745/覆盖 0.8063）。详见 results.md。
- **边界决策**：正式模型**不建议加慢变量通道**（点精度显著变差 + 方向判别显著变差，唯一收益
  覆盖 std -6.5% 与 H 的 cqr 重叠且用点精度换）——待主会话审核
