# L: 采样尺度 vs 藻类过程尺度 · 探索标记（whatwedo.md）

- **标记时间**：2026-08-10 14:55（东八区）
- **任务**：L: 采样尺度 vs 藻类过程尺度（exp/model_enhancement/l_daily_scale/）
- **背景（已证实）**：当前模型 3h 采样（T=24=3 天回看 → H=8=24h 视界）；M5 证明藻类过程是天级到
  周级（翻倍 1-3 天、降水滞后 13-20 天、风 20-29 天）。**假设：3h 对天级过程是过采样**——相邻
  3h 高度相关（3h 网格 lag1 自相关≈0.90），T=24 窗口装不下天级过程完整周期，H=8 覆盖不到一次
  翻倍；降采样到日级 + 拉长视界可能更匹配过程尺度，气象信号可能重新浮现（D 的 wind_u 证伪是在
  3h 尺度 + 24h 视界下做的）
- **探索依据**：exp/EXPLORATION_POLICY.md（文件级探索标记）；参考 B7 abs_delta 增量协议与
  D wind_u 变体（唯一差异 = 采样尺度，协议其余完全复用）
- **方法**：H100（sensecore）同一增量 abs_delta 协议下对比 4 尺度 × 3 seed × 30 epoch，17 窗口
  按日历日对齐（训练 730d / 测试 90d / 步长 45d，各尺度按实际天数一致）：
  - `3h`（T24 H8，视界 24h，复现 B7/D base 作锚）
  - `12h`（T24 H4，视界 48h，M5 网格）
  - `1D`（T30 H7，视界 168h，主候选）
  - `1D_wind`（1D + wind_u 通道，看气象是否日级浮现）
  日级/12h 聚合用均值；wind_u 在 3h 逐时刻算后聚合（同 M5）。评估还原 conc 单位：每视界
  CRPS/RMSE + 覆盖 + 各自持久化技能（跨尺度可比核心指标）+ 相同 24h 提前量跨尺度对照
- **产物**：
  - `run_l.py`（实验脚本，本地 CPU + 算力机 H100 冒烟通过）
  - `trydoing.jsonl`（执行日志）
  - `rethinking.md`（思考记录）
  - `results.md`（结论，待全量后补）
  - `results.json`（统计量，无原始数据）
  - `lossing.md`（边界决策，待全量后补）
- **合规**：只输出统计量/CRPS/RMSE；不打印原始数据行；不触碰 rams/ 冻结代码（仅 import 复用）
- **状态**：🔄 进行中（全量 H100 后台运行中，PID 954022，2026-08-10 14:55）
